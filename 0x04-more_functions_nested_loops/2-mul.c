#include "main.h"

/**
 * mul - multiplies two numbers
 * @x: The first number to multiply
 * @y: The second number to multiply with first number
 * Return: The result of multiplication etween x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
